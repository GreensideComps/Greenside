<div
  aria-hidden="true"
  style="height: {{ block.settings.height }}px;"
  {{ block.shopify_attributes }}
></div>

{% schema %}
{
  "name": "t:blocks.all.spacer",
  "tag": null,
  "settings": [
    { "type": "range", "id": "height", "min": 8, "max": 120, "step": 4, "unit": "px", "label": "Height", "default": 24 }
  ],
  "presets": [{ "name": "t:blocks.all.spacer" }]
}
{% endschema %}

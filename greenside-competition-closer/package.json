{
  "name": "greenside-competition-closer",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled Cloudflare Worker that closes a Greenside competition by zeroing its available inventory once custom.closing_at has passed.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.101.0"
  }
}
